apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "assemblyline.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "assemblyline.name" . }}
    helm.sh/chart: {{ include "assemblyline.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "assemblyline.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
